{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: Build Balatro Game",
            "command": "PowerShell",
            "args": [
                "-File",
                "${workspaceFolder}\\build_game.ps1"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Mengompilasi semua file .cpp di dalam folder src (exclude test_main.cpp)."
        }
    ],
    "version": "2.0.0"
}